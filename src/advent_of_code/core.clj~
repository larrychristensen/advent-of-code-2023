(ns advent-of-code.core
  (:require [clojure.java.io :as io]))

(defn digits-in-word [word]
  (re-seq #"\d" word))

(defn sum-of-calibration-values [filename]
  (with-open [reader (io/reader (io/resource filename))]
    (transduce
     (comp
      (map ))
     +
     (line-seq reader))))
